<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h1><%= t('.edit_test', title: @test.title) %></h1>
    <div class="card border-primary">
      <div class="card-body">
        <%= render 'form' %>
      </div>
      <%= link_to t('.back'), admin_tests_path %>
    </div>
  </div>
</div>
