<div class="row justify-content-center mt-3">
  <div class="col col-md-6 ">
    <h1><%= t('.header') %></h1>
    <table class="table">
      <thead>
        <th class="sort-by-title">
          <%= t('.badge') %>
        </th>
        <th><%= t('.file_name') %></th>
        <th><%= t('.image') %></th>
        <th colspan="2"><%= t('.action') %></th>
      </thead>
      <tbody>
        <%= render @badges %>
      </tbody>
    </table>
    <%= link_to t('.new'), new_admin_badge_path %>
    <br>
    <%= link_to t('.rules'), admin_budge_rules_path %>
    <br>
    <%= link_to t('.back'), admin_tests_path %>
  </div>
</div>
