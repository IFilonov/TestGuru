<div class="row justify-content-center mt-3">
  <div class="col col-md-6 ">
    <h1><%= t('.header') %></h1>
    <table class="table">
      <thead>
        <th class="sort-by-title">
          <%= t('.feedback') %>
        </th>
        <th><%= t('.user') %></th>
        <th colspan="1"><%= t('.action') %></th>
      </thead>
      <tbody>
        <%= render @feedbacks %>
      </tbody>
    </table>
    <%= link_to t('.back'), :back %>
  </div>
</div>
